public abstract class Nepriatel extends Postava {
    public Nepriatel(String meno, int maxHp, int maxMana) {
        super(meno, maxHp, maxMana);
    }
    // Additional enemy-specific behaviors can be added here
}